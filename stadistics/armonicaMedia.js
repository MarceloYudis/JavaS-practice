const listado = [
    1,
    10,
    33,
    5,
    77,
    21,
    11
]

// media armonica = cantidad de elementos / ( suma de los reciprocos de cada elemento )

for(i = 0; i < listado.length; i++) {
    let sumaReciproca = sumaReciproca + 1/listado[i]; 
    
}


const armonicaMedia = listado.length / sumaReciproca;
